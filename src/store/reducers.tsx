const initialState = {
    // Define your initial state here
  };
  
  const rootReducer = (state = initialState, action: any) => {
    switch (action.type) {
      // Handle your actions and update the state accordingly
      default:
        return state;
    }
  };
  
  export default rootReducer;
  